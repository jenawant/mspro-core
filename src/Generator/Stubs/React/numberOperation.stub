
const numberOperation = (newValue, id, numberName) => {
  {BUSINESS_EN_NAME}.numberOperation({ id, numberName, numberValue: newValue }).then( res => {
    res.success && message.success(res.message)
  }).catch( e => { console.log(e) } )
}